{
  "name": "atelier-codia-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "check": "npm run build && node scripts/check-dist.mjs"
  },
  "devDependencies": {
    "vite": "^7.2.0"
  }
}
